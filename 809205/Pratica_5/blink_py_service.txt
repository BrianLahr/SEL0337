[Unit]
Description= Blink LED em Python  # Descrição do serviço que será exibida no sistema
After=multi-user.target  # Define que este serviço deve ser iniciado após o multi-user target

[Service]
ExecStart=/usr/bin/python3 /home/sel/809205/Pratica_5/blink_RPi.py  # Comando para iniciar o serviço com Python
ExecStop=/usr/bin/python3 /home/sel/809205/Pratica_5/blink_finish.py  # Comando para parar o serviço, chamando outro script Python
User=SEL  # Define que o serviço será executado sob o usuário SEL

[Install]
WantedBy=multi-user.target  # Define que este serviço deve ser iniciado no nível de execução multi-user